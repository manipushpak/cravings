export class Vendor {
   constructor(name, phone, address, coordinates, keywords) {
     this.name = name;
     this.phone = phone;
     this.address = address;
     this.coordinates = coordinates;
     this.keywords = keywords;
   }
 }